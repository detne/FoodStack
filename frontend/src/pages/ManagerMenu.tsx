/**
 * Manager Menu
 * Menu status management for branch managers
 * Managers can only toggle availability status for their branch
 */

import ManagerMenuStatus from './ManagerMenuStatus';

export default function ManagerMenu() {
  return <ManagerMenuStatus />;
}
